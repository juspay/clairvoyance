"""Modules whose logic is not final yet. Same skeleton and laws as
app/crm/<module>/; the boundary guard treats app/crm/preview/<module>/
as module <module>. A module moves to app/crm/<module>/ when it settles.
"""
