"""Onboarding surface: install-time bare provisioning and the console stream.

Thin over the engine; ``service.py`` still holds the pre-engine
implementation and shrinks as stages move into ``assist/engine/``.
"""
